# Handoff packet

> Generated by the `handoff-packet` skill.
> Required fields are numbered 1–14 and must all be populated (or marked
> `n/a` with a one-line reason). Vague handoffs are rejected by the skill.

- **Packet timestamp (UTC):** <YYYY-MM-DDTHHMMSSZ>
- **Source agent:** <AGENT_ID>
- **Target agent:** <AGENT_ID>
- **Approval required:** <yes | no>

## 1. Task ID

<TASK_ID>

## 2. Source agent

<AGENT_ID>

## 3. Target agent

<AGENT_ID>

## 4. Objective

<one sentence: what the target must achieve>

## 5. Context summary

<paragraph or bullets: background the target needs>

## 6. Files read

- `<path>` — <why read>
- `<path>` — <why read>

## 7. Files changed

- `<path>` — <one-line summary of change>
- `<path>` — <one-line summary of change>

## 8. Commands run

- `<command>` — exit `<code>` — <one-line result>
- `<command>` — exit `<code>` — <one-line result>

## 9. Validation results

- **Passed:** <list or `none`>
- **Skipped:** <list or `none`, with reason>
- **Failed:** <list or `none`, with pointer to logs>

## 10. Decisions made

- `<DECISION_ID>` — <one-line summary>
- `none` if no material decisions were made this slice.

## 11. Risks

- `<risk>` — likelihood `<low|med|high>` — impact `<low|med|high>`
- `none` if no material risks are known.

## 12. Blockers

- `<BLOCKER_ID>` — <status> — <one-line summary>
- `none` if no blockers are open.

## 13. Required next action

<concrete next step the target must take; not a vague instruction>

## 14. Approval required

<yes | no>

If `yes`: the source retains ownership until the target writes an
`## Acceptance` section below confirming they have read the packet, have
the context they need, and accept the transfer.

## Acceptance

<filled in by the target agent when approving the transfer>

- **Accepted by:** <AGENT_ID>
- **Accepted at:** <ISO-8601 UTC>
- **Notes:** <optional>
