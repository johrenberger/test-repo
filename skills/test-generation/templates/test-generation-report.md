# Test generation report

> Generated by the `test-generation` skill. Lists tests added/modified,
> the framework used, the behaviors covered, and the validation result.

- **Task ID:** <TASK_ID>
- **Repo root:** <REPO_ROOT>
- **Scope:** <SCOPE>
- **Generated at:** <ISO-8601 timestamp>

## Framework used

`<framework>` — detected from <source>. Matched existing repo style.

## Tests added

| Path | Test type | Behaviors covered |
| --- | --- | --- |
| <path> | <type> | <list> |

## Tests modified

| Path | Reason | Cases added |
| --- | --- | --- |
| <path> | <why modify> | <list> |

## Behaviors covered

| Behavior | Cases | Risk | Notes |
| --- | --- | --- | --- |
| <behavior> | <happy, validation, auth, ...> | <critical|high|medium|low> | <notes> |

## Behaviors intentionally not covered

- <behavior> — <reason>

## Validation result (from `validation-runner`)

| Command | Status | Exit | Duration (ms) |
| --- | --- | --- | --- |
| <command> | <passed|failed|partial|skipped> | <int> | <int> |

**Outcome:** `<passed | failed | partial | skipped>`

## Deviations from `test-gap-report.md` (if any)

- <deviation> — <reason>

## Forbidden actions observed

`<none>` or list any framework / dependency changes that were avoided
explicitly (e.g. "did not introduce Testcontainers, repo does not use it
already").

## Follow-up handoff

- **Primary target:** <AGENT_ID, default `TEST_AUTOMATION_AGENT` for
  review, or `SOFTWARE_ENGINEER_AGENT` for fix-and-iterate>
- **Suggested next skill:** `validation-runner` (full suite) → review
- **Handoff packet file:** `handoffs/<UTC-ts>-test-generation-to-<target>.md`
  (must be created with the `handoff-packet` skill)

## Provenance

- Skill: `test-generation`
- Reference profile used: <path to references/*.md>
- Output path: `/data/.openclaw/workspace/tasks/<TASK_ID>/reports/test-generation-report.md`
